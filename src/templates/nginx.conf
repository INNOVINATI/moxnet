http {
  index page0.html;
{% for site in sites %}
  server {
    listen 80;
    root html;
    server_name {{ site.subdomain }}.moxnet.local;
    location /var/www/{{ site.subdomain }} {
        try_files $uri $uri/ $uri.html =404;
    }
  }
{% endfor %}
}